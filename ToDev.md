## Version control:
### to change in every upload:
- In `index.html`:
  - Change the version number in the stylesheet and script links (`style.css` & `script.js`) (e.g., `style.css?v=0.0.1` to `style.css?v=0.0.2`).